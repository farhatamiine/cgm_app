import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const bgPrimary = Color(0xFFF5F4F1);
  static const bgCard = Color(0xFFFFFFFF);
  static const bgElevated = Color(0xFFFAFAF8);
  static const bgMuted = Color(0xFFEDECEA);

  // Text
  static const textPrimary = Color(0xFF1A1918);
  static const textSecondary = Color(0xFF6D6C6A);
  static const textTertiary = Color(0xFF9C9B99);
  static const textDisabled = Color(0xFFD1D0CD);

  // Borders
  static const borderSubtle = Color(0xFFE5E4E1);
  static const borderStrong = Color(0xFFD1D0CD);

  // Accent coral
  static const accentCoral = Color(0xFFD89575);
  static const accentCoralLight = Color(0xFFF5EAE3);

  // Accent green
  static const accentGreen = Color(0xFF3D8A5A);
  static const accentGreenDark = Color(0xFF4D9B6A);
  static const accentGreenLight = Color(0xFFC8F0D8);

  // Accent red (hypo / low)
  static const accentRed = Color(0xFFD08068);
  static const lowHypoBg = Color(0xFFF5E8E3);

  // Warning (high)
  static const accentWarning = Color(0xFFD4A64A);
  static const highBg = Color(0xFFF5F0E3);

  // Navigation
  static const navInactive = Color(0xFFA8A7A5);

  // Shadow
  static const shadowColor = Color(0x081A1918);
  static const shadowColorDark = Color(0x1A000000);
}
